<% layout('/layout/boilerplate') %>
    <div class="row">
        <div class="col-6 offset-3">

            <div class="alert alert-danger" role="alert">
                <h1> Error! <%= err.statusCode %>
                </h1>
                <% if(process.env.NODE_ENV !=="production" ) { %>
                    <hr>
                    <%= err.message %>
                <% } %>
            </div>
        </div>
    </div>